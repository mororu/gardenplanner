# Abhängigkeiten
node_modules/

# Build-Ausgaben
build/
.svelte-kit/
dist/

# Umgebung und Geheimnisse
.env
.env.*
!.env.example

# Datenbank
data/
*.sqlite
*.sqlite-shm
*.sqlite-wal

# Sperrverzeichnis von scripts/backup.sh. Ein sauberer Lauf räumt es selbst
# weg; nach einem harten Abbruch bleibt es liegen und will von Hand entfernt
# werden — im Repository hat es nie etwas zu suchen.
.backup.lock/

# Betriebssystem und Editor
.DS_Store
*.swp

# Ablage für Zugetragenes — Mockups, Bildschirmfotos, alles, was zum Anschauen
# hereinkommt und nicht zum Baum gehört. Wird weder versioniert noch geprüft.
temp/
Bildschirmfoto*.png
