---
epic: 2
date: 2026-08-28
verdict: accepted-with-open-items
criteria: declared
headless: false
---

# Retrospektive Epic 2 — Der Monatsplan landet in der Liste

## Epic-Zusammenfassung

**Epic:** 2 — Der Monatsplan landet in der Liste (FR4, FR5)
**Diff-Bereich:** `e4975b2^..8686692` (= `b207331..8686692`), 4 Commits, keine Merges
**Stories:** beide `done`, keine offene Story (`pending_stories` leer, `sprint_status.py detect-epic --epic 2`)

| Story | Feature-Commit | Abnahme-Commit |
| --- | --- | --- |
| 2.1 Monatsplan in einem Zug ablegen | `e4975b2` | `7b1d9b9` |
| 2.2 Überfällige Aufgaben erkennen | `72a26db` | `8686692` |

**Umfang:** 22 Dateien, +4 530 / −189 Zeilen. Neu: `src/routes/monatsplan/` (922 Zeilen), `src/lib/zeit.ts` (286), `src/lib/aufgabentext.ts` (145), Migration `drizzle/0002_organic_crystal.sql` (eine Zeile, `tasks.due_at`).

### Belegbestand

| Beleg | Vorhanden | Ort |
| --- | --- | --- |
| Epic-Akzeptanzkriterien | ja, **deklariert** | `epics.md:364-427` |
| Epic-Kontext | ja | `epic-2-context.md` |
| Story-Specs | ja, beide | `spec-2-1-…md`, `spec-2-2-…md` |
| Architektur | ja | `ARCHITECTURE-SPINE.md` (AD-8, Quellbaum, Capability-Map) |
| Sprint-Status | ja | `sprint-status.yaml` |
| Vorherige Retrospektive | ja | `epic-1-retro-2026-08-27.md`, 14 Aktionspunkte |
| Zurückgestellte Arbeit | ja | `deferred-work.md`, 41 Einträge |
| **Sitzungsprotokolle** | **nein** | — |

### Bewusste Eingrenzungen dieses Durchgangs

- **`bmad-review` wurde nicht als eigener Lauf über den Epic-Diff gefahren.** Die Code-Ebenen (adversarial, Randfälle, Prüflücken) sind statt dessen hier von Hand über den vollen Diff gelaufen, mit Schwerpunkt auf den Nahtstellen zwischen 2.1 und 2.2 (`zeit.ts`, `queries/tasks.ts`, `+page.svelte`) — dort, wo keine Story-Sitzung beide Seiten sah. Beide Stories haben in ihrem eigenen Schritt 4 bereits drei Review-Ebenen durchlaufen; der Review zu 2.2 ist im Commit-Text von `72a26db` und in vier Einträgen in `deferred-work.md` dokumentiert.
- **Keine Sitzungsprotokolle.** Warum eine Sitzung eine Wendung nahm, ist nirgends festgehalten; die Prozessbefunde unten stützen sich allein auf Commits, Specs und `deferred-work.md`. Dieselbe Lücke wie in Epic 1.
- **Die Story-Zuordnung von `git_evidence.py` blieb leer**, weil die Commit-Betreffs `2.1`/`2.2` mit Punkt schreiben und das Skript `2-1`/`2-2` sucht. Die Zuordnung ist darum von Hand aus den Betreffs gemacht — sie ist eindeutig.

---

## Befunde

### Architektur-Delta

**A1 — Zwei neue geteilte Module an der Wurzel von `src/lib/`, die der geplante Quellbaum nicht kennt.**
`src/lib/zeit.ts` und `src/lib/aufgabentext.ts` liegen bewusst weder unter `client/` noch unter `server/`: beide werden von beiden Seiten gelesen und müssen über nacktes Node ladbar sein (`zeit.ts:17-22`, `aufgabentext.ts:22-26`). Die Begründung trägt. Der Quellbaum in `ARCHITECTURE-SPINE.md:249-278` führt unter `lib/` aber nur `server/`, `client/utils/date.ts` und `components/` — eine dritte Kategorie „von beiden Seiten gelesen, ohne Abhängigkeiten" gibt es dort nicht. Neu ist auch die Richtung `src/lib/client/utils/date.ts:1 → ../../zeit.ts`.
Weder im Plan nachgezogen noch in `deferred-work.md` benannt. Keine Zyklen, keine Schichtverletzung — `zeit.ts` hängt von nichts ab.
*Disposition:* **Spec-Abgleich**, gebündelt mit dem offenen Epic-1-Punkt B9. *Upstream:* siehe P1.

### Duplikationskarte

**D1 — Retro-Posten B5 hat sich messbar verschlechtert.** Gemessen im Baum, beide Stände:

| Was | Ende Epic 1 | Heute | Neue Fundstelle |
| --- | --- | --- | --- |
| `.seitentitel` | 5 | **6** | `monatsplan/+page.svelte:551` |
| `.seite` | 4 | **5** | `:543` |
| `.fehler` | 3 | **4** | `:566` |
| `.live:empty` | 3 | **4** | `:580` |
| `abweisen`-Signaturen | 3 | **4** | `monatsplan/+page.server.ts:84` |
| `request.formData()` ohne try/catch | 6 | **7** | `monatsplan/+page.server.ts:132` |

Der Code benennt es selbst: `monatsplan/+page.svelte:537-542` schreibt „Dies ist die vierte Kopie; die Zusammenlegung nach `bedienelemente.css` ist offener Retro-Posten B5 und betrifft auch `/verwaltung` — sie gehört nicht in diese Story." Das ist ehrlich und im Sinne der Story-Grenze richtig; die Kosten des offenen Postens steigen trotzdem mit jeder Seite.
Der Gap-Drift besteht unverändert: `verwaltung/+page.svelte:455` hat `gap: var(--space-5)`, die vier anderen `.seite` haben `--space-4`.
*Disposition:* **jetzt beheben** — die Epic-1-Punkte 9 und 10 bleiben stehen und sind jetzt eine Seite breiter.

**D2 — Vier Formen von `abweisen` für dieselbe Sache.**
`+page.server.ts:33` `()` · `aufgabe/+page.server.ts:92` `(meldung, eingabe)` · `monatsplan/+page.server.ts:84` `(meldung, feld: 'datum'|'zeilen')` · `verwaltung/+page.server.ts:128` `(meldung, feld, nameEingabe)`. Die vierte ist die sauberste (kein Echo der Eingabe, weil der Text im `$state` der Komponente steht) — sie macht die Drift aber breiter statt schmaler.
*Disposition:* **jetzt beheben**, zusammen mit D1.

### Grössenwachstum

**G1 — Das Prüfwerkzeug ist um 1 561 Zeilen gewachsen, während der Entscheid über seinen Status (B8) offen blieb.** Gleich gemessen an beiden Ständen (`ab259f8` gegen `8686692`, `src/` = `.ts`/`.svelte`/`.html`/`.css`):

| | Ende Epic 1 | Heute | Delta |
| --- | --- | --- | --- |
| `src/` | 4 316 | 6 031 | +1 715 (+40 %) |
| `gate.mjs` + `smoke-zugang.ts` + `db-check.ts` | 4 951 | 6 512 | +1 561 (+32 %) |
| davon `smoke-zugang.ts` | 2 781 | **4 342** | +1 561 (+56 %) |
| `README.md` | 1 443 | 1 807 | +364 |

Nur `smoke-zugang.ts` ist gewachsen; `gate.mjs` und `db-check.ts` blieben unangetastet. Das Verhältnis Werkzeug zu Anwendung ist von 1,15 auf 1,08 gefallen — das Werkzeug wächst also nicht mehr überproportional, es ist aber weiterhin **grösser als die Anwendung** und kommt in `epics.md`, `ARCHITECTURE-SPINE.md`, `DESIGN.md` und `SPEC.md` nicht vor.
*Disposition:* **Entscheid** — Epic-1-Punkt 7 bleibt offen, sein Gegenstand ist um ein Drittel gewachsen.

**G2 — `smoke-zugang.ts` trägt eine Reihenfolgefalle, die nur ein Kommentar hält.** Der 2.2-Block sät Zeilen mit `created_at` bis 60 Tage in der Vergangenheit; weil `offeneAufgabenAuflisten` nach `created_at` aufsteigend sortiert, stellen sie sich vor alles Frühere und machen vier Behauptungen mit exakten Id-Ketten aus 1.4/1.5 rot. Heute stimmt es, weil der Block der letzte ist. Wer die nächste Story anhängt, tappt hinein, und die Meldung zeigt auf eine fremde Story.
Bereits benannt in `deferred-work.md:131`. Zwei Auswege dort genannt; der 2.2-Block macht es über `gesäteIds` schon richtig.
*Disposition:* **zurückgestellt**, aber vor Epic 3 fällig — Epic 3 hängt Behauptungen an.

**G3 — Zwei Komponenten über 700 Zeilen.** `monatsplan/+page.svelte` 753, `+page.svelte` 711. Beide sind zu grossen Teilen Begründungsprosa im Hausstil und nicht Logik; die Logikanteile sind je klein und klar geschnitten. Kein Handlungsbedarf, hier nur gemessen festgehalten, damit die nächste Retro die Kurve hat.
*Disposition:* **so hinnehmen.**

### Musterabweichung

**M1 — Die Lehren aus Epic 1 sind nach vorn gewandert. Das ist der klarste Gewinn dieses Epics.**
`/monatsplan` fasst die `imFlug`-Freigabe in `try/finally` und nennt den Grund beim Namen: „Retro-Posten B1 aus Epic 1" (`monatsplan/+page.svelte:317-326`). Der Fehlersatz steht als immer vorhandene Live-Region im Markup (`:371`), also genau in der Form, die B2 an `/verwaltung` vermisst hat — belegt durch die ausgeführte Behauptung „der Fehlersatz auf /monatsplan ist eine immer vorhandene Live-Region".
*Disposition:* **so lassen**, als Muster festhalten.

**M2 — Die ältere Fundstelle derselben Klasse blieb trotzdem stehen.**
`verwaltung/+page.svelte:345-346` rendert den Feldfehler weiter bedingt hinter `{#if}` und ohne `role`/`aria-live` — unverändert seit der Epic-1-Retro, die genau diese Zeilen als B2 benannt hat. Die obere Live-Region derselben Seite (`:276-281`) ist richtig; der Feldfehler daneben ist es nicht.
Prozesslehre 13 der Epic-1-Retro („eine entdeckte Gefahr löst eine Prüfung der früheren Fundstellen aus") hat also nach vorn gewirkt, nicht nach hinten.
*Disposition:* **jetzt beheben** — Epic-1-Punkt 2 bleibt offen.

### Spec-Ist-Abgleich

**S1 — Eine Vorbedingung, die das Projekt sich selbst gesetzt hat, wurde nicht eingelöst.**
Der Eintrag aus Story 2.1 (`deferred-work.md:99-100`) schreibt zur fehlenden Plausibilitätsschranke an `Fällig bis`: „Gehört entschieden, **bevor** Story 2.2 die Überfälligkeit rechnet." Story 2.2 hat gerechnet, ohne dass die Frage entschieden wurde. Sie führt die Schranke unter „Ask First" und macht aus der Folge ein Merkmal: `seit ~1900 Wochen offen` ist ausdrücklich als Diagnosesignal gewollt und gegen eine Kappung begründet (`zeit.ts:243-250`, `spec-2-2` Design Notes).
Die gebaute Lösung ist vertretbar. Der Punkt ist nicht das Ergebnis, sondern dass die Entscheidung nicht getroffen, sondern umgangen wurde — und `deferred-work.md` die Stelle war, an der sie stand.
*Disposition:* **Entscheid** (Produktfrage). *Upstream:* siehe P2.

**S2 — `seit N Wochen offen` untertreibt bei Planaufgaben systematisch.**
Eine vor 60 Tagen angelegte Aufgabe mit Fälligkeit vor 25 Tagen zeigt `seit 3 Wochen offen`, obwohl sie seit 8,5 Wochen offen liegt; die Matrix der Story schreibt „3, nicht 8" sogar als richtig fest (`spec-2-2`, I/O-Matrix). Der Wortlaut ist doppelt gebunden — `epics.md:412` und `DESIGN.md:199,248` schreiben den Satz wörtlich vor, AD-8 schreibt die Zahl über `COALESCE(due_at, created_at)` wörtlich vor. Beide zu erfüllen heisst, die Doppeldeutigkeit zu erben; die Umsetzung hält sie in Komponente und README ausdrücklich fest (`+page.svelte:75-84`).
Benannt in `deferred-work.md:130`.
*Disposition:* **Entscheid** — entweder `seit N Wochen überfällig` (dann ziehen Epic-AC, `DESIGN.md` und `EXPERIENCE.md` nach), oder die Untertreibung wird als gewollt abgenommen.

**S3 — `EXPERIENCE.md:79,101` verweist auf ein Token `{colors.warn}`, das es nicht gibt.**
Das Token heisst `overdue` (`src/app.html:73,206`); `DESIGN.md:248` schreibt es richtig. Kein Zustand hängt daran — die gebaute Zeile benutzt `var(--overdue)`, und Gate-Regel 3 wiese ein `var(--warn)` sofort ab. Dieselbe Datei benutzt `{colors.warn}` daneben für „Dienstwoche unbesetzt" (`:102`), was Epic 3 betrifft und mitentschieden werden muss.
Bewusst nicht in der Story korrigiert, und die Begründung ist strukturell: eine Korrektur in `EXPERIENCE.md` machte `epic-2-context.md` ungültig, gegen das gebaut wurde. Benannt in `deferred-work.md:113-114`.
*Disposition:* **Spec-Abgleich.** *Upstream:* siehe P1.

**S4 — Der ausgelieferte Entwicklerkommentar (B4) steht nicht nur noch da, er ist jetzt auch veraltet.**
`src/app.html:13-28` liefert weiter zehn Zeilen Entwicklerprosa mit internen Dateipfaden an jeden Besucher aus. Er zählt die Seiten mit eigenem `<title>` auf — `+page.svelte`, `mehr`, `verwaltung`, `+error` — und `/monatsplan` fehlt darin, obwohl die Seite ihren eigenen Titel trägt (`monatsplan/+page.svelte:337-339`). Gate-Regel 12 fasst diesen Fall weiterhin nicht.
*Disposition:* **jetzt beheben** — Epic-1-Punkt 4 bleibt offen und hat an Aktualität verloren.

### Diff-Durchsicht (Code-Ebenen, von Hand über den Epic-Diff)

Keine Korrektheitsfehler gefunden, die über die schon benannten hinausgehen. Geprüft und sauber:

- **`tagesendeInUnixSekunden`** (`zeit.ts:135-176`): der Rückvergleich der Zonenbestandteile trägt zwei Lasten zugleich — er belegt, dass eine Runde genügt, und weist ein unmögliches Datum wie `2026-02-31` ab, das `Date.UTC` sonst still auf den 3. März rollte. Ausgeführt belegt durch smoke.
- **`monatsendeAlsFeldwert`** (`:102-106`): `Date.UTC(jahr, monat, 0)` mit eins-basiertem `monat` ist der letzte Tag des laufenden Monats; Schaltjahre rechnen sich von selbst.
- **`wochenOffenSeit`** (`:279-283`): strikter Vergleich, negative Differenz braucht keinen Sonderfall, kleinster Rückgabewert 3 — an der Schwelle, eine Sekunde darüber und bei negativer Differenz ausgeführt belegt.
- **Zähler und Server benutzen dieselbe Funktion** (`zeilenErkennen`), und die Grenzen liegen im geteilten Modul. Der Knopf `24 Aufgaben ablegen` kann nicht 23 anlegen.
- **`aufgabenStapelAnlegen`** (`queries/tasks.ts:157-163`): ein mehrzeiliges INSERT ist in SQLite atomar, 200 gebundene Parameter bei `PLAN_HOECHSTZAHL = 100` liegen unter beiden SQLite-Schranken. Die Kopplung ist am Ort benannt.
- **Die Refaktorierung von `aufgabe/+page.server.ts`** auf das geteilte Modul ist verhaltensgleich: dieselbe Faltungskette, dieselbe Codepoint-Zählung, dieselben zwei Sätze.
- **`aria-describedby`** sitzt nur am `abhaken`-Kästchen, weil `istUeberfaellig` `!istErledigt` enthält und das `wiederOeffnen`-Formular nur bei `istErledigt` gerendert wird — die zwei Bedingungen schliessen sich aus.

Ein toter Zweig ohne Folgen: `zwischentext` (`monatsplan/+page.svelte:127-134`) hat einen Zweig `„noch ohne Frist"`, der unerreichbar ist, weil `Weiter` bei ungültigem Datum sperrt. Defensiv und billig — kein Handlungsbedarf.

### Prozess

**P1 — Ein struktureller Widerspruch blockiert den Spec-Abgleich.**
Epic-1-Punkt 12 verlangt, den Plan beim Story-Abschluss nachzuziehen oder die Abweichung in `deferred-work.md` zu benennen. Die Story-Specs verbieten zugleich, Planungsartefakte anzufassen — „Never: Planungsartefakte anfassen — jede Änderung dort macht `epic-2-context.md` ungültig" (`spec-2-2`). Der Abgleich kann darum nur die zweite Form annehmen. In diesem Epic hat das einmal funktioniert (S3, sauber notiert mit Begründung) und einmal nicht (A1, gar nicht notiert).
*Disposition:* **Prozess.** Der Widerspruch ist auflösbar: ein Abgleich-Schritt **zwischen** den Epics, bei dem der Kontext ohnehin neu kompiliert wird.

**P2 — `deferred-work.md` wächst und wird nicht triagiert.**
Epic-1-Punkt 11 lautete „die 30 Einträge triagieren, **bevor** Epic 2 beginnt". Ausgeführt wurde er nicht; die Datei steht heute bei 41 Einträgen (11 aus Epic 2: 4 aus 2.1, 2 aus 2.2, 5 aus dem Review zu 2.2). Sie wird nie geleert. Darin stecken inzwischen mindestens vier ungetroffene Produktentscheidungen (S1, S2, die fehlende Umbruchregel, der Verlust des getippten Plans bei einem Wurf in der action), und S1 zeigt, dass ein Eintrag mit ausdrücklicher Fälligkeit folgenlos verstreicht, wenn niemand die Datei vor der nächsten Story liest.
*Disposition:* **Prozess** — die Triage vor Epic 3, und ein fester Lesepunkt beim Story-Start.

**P3 — Das Abnahmemuster wiederholt sich zum dritten Mal: abgenommen wird die sichtbare Funktion, nicht die Barrierefreiheit.**
Für Story 2.2 sind laut `deferred-work.md:137` fünf von sechs Prüflistenpunkten **nicht** einzeln bestätigt: dunkler Modus bei 375px, 44px-Trefferfeld bei der jetzt zweizeiligen Zeile, Beurteilung des Höhensprungs beim Abhaken, VoiceOver (Name gegen Beschreibung) und Lesbarkeit bei ausgeschalteter Farbdarstellung. Der Handy-Test wurde ausdrücklich verschoben. Drei der acht Akzeptanzkriterien der Story hängen genau an diesen Punkten, und die Svelte-Schicht ist in diesem Projekt von keinem ausgeführten Werkzeug gedeckt — dort tragen fünf Textprüfungen alles.
Der Eintrag benennt das Muster selbst und verweist auf Epic 1 und Story 2.1.
*Disposition:* **Prozess** — entweder eine schmale Prüfstufe, die die Lücke schliesst (Stufe A/C aus dem vierstufigen Vorschlag), oder die Barrierefreiheitspunkte werden ausdrücklich als ungeprüft abgenommen statt stillschweigend mitgenickt.

**P4 — Weiterhin keine Sitzungsprotokolle.** Wie in Epic 1. Alles oben stützt sich auf Commits, Specs und `deferred-work.md`.

### Was gut lief

- **Der Ablauf ist so schlank wie am Ende von Epic 1 und blieb es:** je Story ein Feature-Commit plus Abnahme, keine Nachbesserungsrunde in eigenen Commits. Vier Commits im ganzen Epic.
- **Die Retro-Lehren sind in den neuen Code eingegangen** (M1), mit Verweis auf den Posten — nicht zufällig, sondern nachlesbar begründet.
- **Der Review zu 2.2 hat zwei Prüflücken vorgeführt, die grün durchliefen** (`aria-describedby` am falschen Kästchen; die zweite Textzeile über dem Aufgabentext). Beide wurden geschlossen, indem die Prüfungen den Quelltext in Bereiche schneiden statt über die ganze Datei zu greppen. Das ist genau die Art Fund, die eine gewöhnliche Story-Prüfung nicht macht.
- **AD-8 ist buchstäblich eingehalten und ausgeführt belegt:** die Schwelle steht genau einmal, es gibt kein `is_overdue` in `src/` oder `drizzle/`, keinen Timer und keine Migration. Die Hinweiszahl von `gate` ist wie im Akzeptanzkriterium gefordert von 2 auf 1 gefallen.
- **`smoke` von 262 auf 374 Behauptungen**, jede neue Zusage durch eine Mutationsprobe rot belegt.

---

## Verhaltensprüfung

Ausgeführt am 2026-08-28 auf dem Stand `8686692`, nicht abgeleitet:

| Befehl | Ergebnis |
| --- | --- |
| `npm run lint` | **Exit 0** — Prettier, ESLint, `gate`, `gate:selftest`, `db:check`, `db:check:selftest`, `smoke` |
| davon `gate` | 33 Dateien, 61 Tokens, **1 Hinweis** (`--border-marker` unbenutzt), dreizehn Regeln erfüllt |
| davon `smoke` | **374 Behauptungen** ausgeführt, darunter jede Zeile der Überfälligkeitsmatrix |
| `npm run check` | 635 Dateien, **0 Fehler, 0 Warnungen** |
| `env -u DATABASE_PATH -u SESSION_SECRET -u ORIGIN npm run build` | **Exit 0** |

Damit sind die Akzeptanzkriterien „`gate` mit 1 statt 2 Hinweisen", „Schwelle an genau einer Stelle", „kein `is_overdue`, kein Cron, kein Job" und „`db:check` meldet keine Schemaänderung" maschinell belegt.

**Nicht in diesem Durchgang ausgeführt:** die laufende Anwendung im Browser. Die visuelle Prüfung beider Stories liegt als mündliche Abnahme durch Manuel vor und ist mit ihrem Umfang **und ihren Lücken** in `deferred-work.md:107` und `:137` festgehalten — inklusive der selbst angelegten Planaufgabe mit Fälligkeit 2026-07-01, die `seit 8 Wochen offen` zeigte, und der dabei entschiedenen Produktfrage (eine Frist in der jüngeren Vergangenheit trägt bewusst keinen Hinweis, die Schwelle bleibt bei drei Wochen).

---

## Nachverfolgung der Aktionspunkte aus Epic 1

14 Punkte aus `epic-1-retro-2026-08-27.md`, alle mit Status `open` in `sprint-status.yaml`.

| # | Punkt (id) | Gelandet? | Beleg | Vorgeschlagener Status |
| --- | --- | --- | --- | --- |
| 1 | `epic-1-retro-item-1-imflug-freigabe-auf-und-verwaltung-in-tr` | **ja** | Commit `b207331` „fix: imFlug-Freigabe in try/finally auf / und /verwaltung"; `/monatsplan` folgt dem Muster (`monatsplan/+page.svelte:323-326`) | **`done`** |
| 2 | `epic-1-retro-item-2-fehlersatz-in-verwaltung-page-svelte-imm` | nein | `verwaltung/+page.svelte:345-346` unverändert bedingt, ohne `role`/`aria-live` (M2) | — |
| 3 | `epic-1-retro-item-3-namenspruefung-in-scripts-create-admin-t` | nein | `scripts/create-admin.ts:43` weiterhin `.replace(/\s+/g,' ').trim()`, ohne Nullbreiten-Filter und ohne Längengrenze | — |
| 4 | `epic-1-retro-item-4-entwicklerkommentar-aus-src-app-html-ent` | nein | `src/app.html:13-28` unverändert ausgeliefert, jetzt zusätzlich veraltet (S4) | — |
| 5 | `epic-1-retro-item-5-ad-12-und-stack-tabelle-auf-die-gebaute` | nein | keine Planungsartefakte im Epic-2-Diff angefasst (`git diff --name-only`, null Treffer unter `planning-artifacts/`) | — |
| 6 | `epic-1-retro-item-6-ad-13-von-beim-modulladen-auf-beim-start` | nein | dito | — |
| 7 | `epic-1-retro-item-7-entscheiden-pruefwerkzeug-in-die-archite` | nein | Werkzeug um 1 561 Zeilen gewachsen, in keinem Plandokument (G1) | — |
| 8 | `epic-1-retro-item-8-restliche-plan-ist-abweichungen-in-einem` | nein | dito; A1 kommt als neue Abweichung dazu | — |
| 9 | `epic-1-retro-item-9-geteilte-stile-nach-bedienelemente-css-z` | nein, **verschlechtert** | vier neue Kopien in `monatsplan/+page.svelte:543,551,566,580`; Gap-Drift in `verwaltung:455` unverändert (D1) | — |
| 10 | `epic-1-retro-item-10-die-drei-abweisen-signaturen-auf-eine-fo` | nein, **verschlechtert** | vierte Signatur in `monatsplan/+page.server.ts:84` (D2) | — |
| 11 | `epic-1-retro-item-11-die-30-eintraege-in-deferred-work-md-tri` | nein | `deferred-work.md` von 30 auf 41 Einträge gewachsen (P2) | — |
| 12 | `epic-1-retro-item-12-story-abschluss-um-einen-spec-abgleich-e` | **teilweise** | S3 wurde sauber notiert, A1 nicht; strukturell blockiert (P1) | — |
| 13 | `epic-1-retro-item-13-entdeckte-gefahr-einer-story-loest-pruef` | **teilweise** | nach vorn gewirkt (M1), nach hinten nicht (M2) | — |
| 14 | `epic-1-retro-item-14-stories-ueber-der-token-empfehlung-vor-d` | nicht anwendbar | beide Epic-2-Stories liefen in je einer Sitzung durch, keine Teilung nötig | — |

Nur für Punkt 1 gibt es einen Beleg, der eine Statusänderung trägt. Manuel hat sie am 2026-08-28 bestätigt; der Punkt steht in `sprint-status.yaml` seither auf `done`. Die übrigen dreizehn bleiben unverändert `open`.

---

## Aktionspunkte

Alle **vorgeschlagen**, nicht ausgeführt. Owner: Manuel.

### Behebung

1. **Fehlersatz in `verwaltung/+page.svelte:345-346` immer ins DOM**, mit `role="alert"` und `aria-live="assertive"` — das Muster steht fertig in `monatsplan/+page.svelte:371` und `aufgabe/+page.svelte:128`. *(M2; ersetzt Epic-1-Punkt 2, jetzt mit Vorlage im Baum.)*
2. **`.seite`, `.seitentitel`, `.fehler`, `.live:empty` nach `bedienelemente.css` ziehen** — jetzt fünf bzw. sechs bzw. vier Kopien; den `.seite`-Gap-Drift in `verwaltung:455` dabei auflösen. *(D1)*
3. **Die vier `abweisen`-Signaturen auf eine Form bringen** — die Fassung aus `/monatsplan` ist die tragfähigste. *(D2)*
4. **Entwicklerkommentar aus `src/app.html:13-28` entfernen** und Gate-Regel 12 auf ausgelieferte Entwicklerprosa erweitern. *(S4)*
5. **Namensprüfung in `scripts/create-admin.ts:43`** an die UI-Kette angleichen. *(Epic-1-Punkt 3, unverändert offen.)*
6. **Die Reihenfolgefalle in `smoke-zugang.ts` entschärfen, bevor Epic 3 Behauptungen anhängt** — die älteren Reihenfolgeprüfungen aus 1.4/1.5 auf eigene Ids umstellen, wie es der 2.2-Block über `gesäteIds` schon tut. *(G2)*

### Entscheid

7. **Plausibilitätsschranke an `Fällig bis`** — Vergangenheit hart abweisen, nur warnen, oder Obergrenze nach vorn. Die Frage war fällig vor Story 2.2 und ist es weiterhin. *(S1)*
8. **Wortlaut `seit N Wochen offen`** — Untertreibung als gewollt abnehmen, oder auf `seit N Wochen überfällig` umstellen und `epics.md:412`, `DESIGN.md:199,248`, `EXPERIENCE.md:79,101` nachziehen. *(S2)*
9. **Prüfwerkzeug: in die Architektur aufnehmen oder zurückbauen.** 6 512 Zeilen, in keinem Plandokument. *(G1; Epic-1-Punkt 7.)*

### Spec-Abgleich

10. **`src/lib/zeit.ts` und `src/lib/aufgabentext.ts` in den Quellbaum von `ARCHITECTURE-SPINE.md` aufnehmen**, als dritte Kategorie „von beiden Seiten gelesen, ohne Abhängigkeiten", mit dem Grund (nacktes Node). *(A1)*
11. **`EXPERIENCE.md:79,101` von `{colors.warn}` auf `{colors.overdue}` korrigieren**; `:102` (Dienstwoche unbesetzt, Epic 3) mitentscheiden. *(S3)*
12. **Die offenen Plan-Ist-Abweichungen aus Epic 1 in einem Durchgang nachziehen** (AD-12/`vite-plugin-pwa`, AD-13/`ORIGIN`, Schriftquelle, `wiederOeffnen`, `neuAusstellen`, `--danger`, certbot, `+layout.server.ts`, `migrations/`) — zusammen mit 10 und 11, während der Epic-Kontext für Epic 3 ohnehin neu kompiliert wird. *(Epic-1-Punkte 5, 6, 8.)*

### Prozess

13. **Einen Abgleich-Schritt zwischen die Epics setzen.** Solange Stories Planungsartefakte nicht anfassen dürfen, kann der Abgleich nur dort stattfinden, wo der Epic-Kontext ohnehin neu entsteht. Damit wird Epic-1-Punkt 12 zum ersten Mal ausführbar. *(P1)*
14. **`deferred-work.md` vor Epic 3 triagieren**, und den Story-Start um einen Lesepunkt ergänzen: Einträge, die auf die kommende Story zeigen, werden vor dem Bau entschieden. S1 ist der Beleg, dass ein Eintrag mit ausdrücklicher Fälligkeit sonst folgenlos verstreicht. *(P2)*
15. **Die Barrierefreiheitspunkte der Abnahme sichtbar machen.** Entweder eine schmale ausgeführte Prüfstufe gegen einen echten Server (Stufe A deckt die HTML-Struktur, Stufe C die Geometrie und die Ansage), oder die nicht geprüften Punkte werden bei der Abnahme einzeln als ungeprüft benannt und abgenommen — nicht stillschweigend mitgenickt. *(P3)*
16. **Sitzungsprotokolle festhalten**, wenigstens die Wendungen. Zum zweiten Mal die einzige Belegklasse, die ganz fehlt. *(P4)*

---

## Abnahmeurteil

**accepted-with-open-items** — Kriterien **deklariert** (`epics.md:370-427`).

**Wofür der Beleg trägt:**

- Beide Stories `done`, `pending_stories` leer.
- Alle maschinell prüfbaren Akzeptanzkriterien beider Stories sind ausgeführt belegt: die Zählung, der zweischrittige Ablauf, ein `due_at` für den ganzen Stapel, die Meldung mit Zahl, die strikte Schwelle, die Monatsplan-Ausnahme, der Vorrang von `due_at` über `created_at`, die unveränderte Sortierung, das Fehlen von `is_overdue`/Cron/Job/Timer, die Schwelle an genau einer Stelle, `gate` mit 1 statt 2 Hinweisen, `db:check` ohne Schemaänderung. `npm run lint`, `check` und `build` heute mit Exit 0 nachgefahren.
- Kein blockierender Befund. Kein Korrektheitsfehler im Epic-Diff gefunden, der nicht schon benannt und begründet wäre.

**Was offen bleibt und darum nicht „accepted" heisst:**

- **NFR3 ist die erklärte Messlatte des Epics** („wenn das Ablegen mehr Aufwand kostet als die Papierliste, tut es die rotierende Person nicht", `epic-2-context.md`) und ist als Akzeptanzkriterium von Story 2.1 deklariert. `deferred-work.md:107` hält fest, dass nicht mitgeteilt wurde, ob dafür ein realer Plan von 20–40 Aufgaben übertragen wurde. Die Story ist mündlich abgenommen — die Abnahme steht, der Beleg für dieses eine Kriterium fehlt.
- **Drei Akzeptanzkriterien von Story 2.2 tragen die Barrierefreiheit** (Trefferfeld ≥ 44px bei der zweizeiligen Zeile in Hell und Dunkel, Kästchenname gegen Beschreibung im Screenreader, Lesbarkeit bei ausgeschalteter Farbdarstellung). Alle drei sind laut `deferred-work.md:137` nicht einzeln bestätigt, und keines ist von einem ausgeführten Werkzeug gedeckt.
- **Zwölf der vierzehn Aktionspunkte aus Epic 1 sind unverändert offen**, zwei davon (D1, D2) haben sich in diesem Epic messbar verschlechtert.

Die Abnahme durch Manuel liegt für beide Stories vor und geht dem maschinellen Urteil vor; das Urteil hält fest, was diese Abnahme ausdrücklich nicht abdeckt.

---

## Offene Fragen

1. **Wurde für Story 2.1 ein echter Monatsplan von 20–40 Zeilen übertragen?** Eine Ja/Nein-Antwort entscheidet, ob NFR3 belegt ist oder ob das Kernkriterium des Epics ungeprüft steht. Bei Nein: ein Durchgang mit einem realen Plan, gemessen gegen die Papiervariante.
2. **`seit N Wochen offen` oder `seit N Wochen überfällig`?** Die Antwort entscheidet, ob drei Plandokumente nachziehen (Punkt 8).
3. **Plausibilitätsschranke an `Fällig bis`:** abweisen, warnen oder nichts? Bis dahin kann ein vertipptes Jahr einen Stapel von bis zu 100 Aufgaben anlegen, den keine Löschen-Aktion aufräumt (Punkt 7).
4. **Prüfwerkzeug: Architektur oder Rückbau?** Der Entscheid steht seit Epic 1 aus, der Gegenstand ist um ein Drittel gewachsen (Punkt 9).
5. **Soll die Barrierefreiheit eine ausgeführte Prüfstufe bekommen?** Solange nicht, hängen die a11y-Zusagen von `/` und `/monatsplan` an fünf Textprüfungen und einer mündlichen Abnahme (Punkt 15).
