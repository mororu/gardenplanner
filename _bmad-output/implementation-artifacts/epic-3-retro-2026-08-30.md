---
epic: 3
date: 2026-08-30
verdict: accepted-with-open-items
criteria: declared
headless: false
---

# Retrospektive Epic 3 — zweiter Lauf

Der erste Lauf (`epic-3-retro-2026-08-29.md`) sprach **`rejected`**, und zwar aus einem einzigen Grund: Story 3.2 stand auf `review`, der von der Workflow-Notiz vorgesehene separate Code-Review in frischer Sitzung war nicht gelaufen. Das Urteil sagte damals selbst, ein erneuter Lauf komme zu einem anderen Spruch.

Dieser Lauf ist dieser erneute Lauf. Er wiederholt die Befunde des ersten nicht — sie stehen dort und gelten — sondern misst **zwei** Dinge nach: ob die Aktionspunkte gehalten haben, was sie versprachen, und was die Fläche zeigt, die seit dem ersten Lauf entstanden ist. Diese Fläche hat **nie einen Review gesehen**, und genau dort liegt der schärfste Befund dieses Laufs.

## Epic-Zusammenfassung

**Epic:** 3 — Verbindlichkeit ohne Nachfragen (FR6, FR7, FR8, FR9, FR10, NFR13)

**Zwei Bereiche, getrennt gemessen:**

| Bereich | Bereich | Commits | Umfang |
| --- | --- | ---: | --- |
| Das Epic selbst | `d6a0af5..a25bda5` | 25 (1 Merge, 1/1 gemessen) | 67 Dateien, +14 565 / −1 656 |
| Seit dem ersten Lauf | `a25bda5..HEAD` | 8 | 19 Dateien, +1 485 / −490 |

Die erste Zeile reproduziert die Messung vom 2026-08-29 exakt — 25 Commits, 67 Dateien, `merges_measured` 1 von 1. Die zweite ist neu und zerfällt in drei Teile:

| Teil | Commits | Reviewt? |
| --- | --- | --- |
| Die Retrospektive selbst | `65067bb` | — |
| **Die Aktionspunkte 3 bis 5** | `2ee5b03`, `4af054f`, `4dfe721`, `6e76288`, `12552f2` | **nein, keiner** |
| Der separate Review zu Story 3.2 | `f76f229`, `af29c40` | ja (Durchgang 2, heute) |

**Stories:** alle vier `done`. `pending_stories` ist leer — der Gate, der den ersten Spruch erzwang, ist offen.

| Story | Status |
| --- | --- |
| 3.0 Das ausgelieferte HTML gegen einen echten Server prüfen | `done` |
| 3.0.1 Einen Mitgliedsnamen korrigieren | `done` |
| 3.1 Dienstplan mit Namen und laufender Woche | `done` |
| 3.2 Einzelaufgabe ausschreiben und übernehmen | `done` (seit heute) |

### Belegbestand

| Beleg | Vorhanden | Ort |
| --- | --- | --- |
| Epic-Akzeptanzkriterien | ja, **deklariert** | `epics.md:434-546` |
| Story-Specs | ja, alle vier | `spec-3-0-…` bis `spec-3-2-…` |
| Architektur / PRD | ja | `ARCHITECTURE-SPINE.md`, `epics.md`, `DESIGN.md`, `EXPERIENCE.md` |
| Sprint-Status | ja | `sprint-status.yaml`, 45 Aktionspunkte |
| Vorgänger-Retro | ja | `epic-3-retro-2026-08-29.md` (dieses Epic), `epic-2-retro-2026-08-28.md` |
| Code-Review Story 3.2 | ja, **neu** | Spec, Abschnitt `Review Findings`, Durchgang 2 |
| Commit-Zuordnung je Story | **nein** | unverändert: die Betreffe folgen Conventional Commits und nennen keine Story-Ids |
| Sitzungsprotokolle | **nein** | unverändert — Posten P4 aus Epic 2, Posten F1 aus diesem Epic |

### Verengte Sichten

- **Kein kopfloser Browser.** Unverändert und dreimal in Folge benannt. Was erst im Browser entsteht — `showModal()`-Fokus, Esc, die Ansage der Live-Regionen, die Sperre gegen Doppelversand — ist ungeprüft. Durchgang 2 hat für die Übernahme *Quelltext*-Behauptungen über die Rückrufrümpfe nachgezogen; das ist schärfer als nichts und ist **keine** Laufzeitprüfung.
- **Die Befunde des ersten Laufs sind nicht neu erhoben.** Sie stehen in `epic-3-retro-2026-08-29.md`; dieser Lauf gleicht sie ab.

---

## Befunde

Jeder Befund nennt seine Quelle, und jede Behauptung eines Unteragenten ist am Primärquelltext nachgeprüft.

### R — Die Fläche, die nie einen Review gesehen hat

**R1 · Eine ausgelieferte Regression: der Kopierstand auf `/verwaltung` hat seinen Regelkörper verloren.** *(fix now — der einzige blockierende Befund dieses Laufs)*

Commit `2ee5b03` zog `.einmal__warnung` in die geteilte Rolle `.hinweis` und entfernte den Regelkörper. Der Selektor davor blieb stehen und klebt seither am nächsten Rumpf:

```css
/* a25bda5 */                        /* HEAD */
.einmal__stand,                      .einmal__stand,
.einmal__warnung {                   .aufnahme {
  margin: 0;                           display: flex;
  color: var(--ink-secondary);         flex-direction: column;
  font-family: var(--meta-font);       gap: var(--space-3);
  font-size: var(--meta-size);       }
  font-weight: var(--meta-weight);
  line-height: var(--meta-line);
}
```

`verwaltung/+page.svelte:718-723` gegen `git show a25bda5:…:727-735`. Betroffen ist `<p class="einmal__stand" role="status">{kopierstand.satz}</p>` (`:399`) — die Rückmeldung nach *Link kopieren*, im Kasten, in dem der Klartext-Link **genau einmal** zu sehen ist. Sie verliert `margin: 0` (die UA-Vorgabe `1em 0` kommt zurück, in einem Flexcontainer mit `gap`), verliert die meta-Rampe, erbt Fliesstext in `--ink-primary` — und wird selbst zum Flexcontainer. Ein CSS-Reset fängt nichts ab: `app.html` setzt nur `body { margin: 0 }`.

**Keine der 755 Behauptungen sieht das.** `grep -rn "einmal__\|kopierstand" scripts/` ergibt nichts; die `SEITENFORM`-Wache führt `.einmal__stand` nicht und liest ohnehin nur Regel**köpfe**; `svelte-check --fail-on-warnings` schweigt, weil der Selektor benutzt wird; `gate` kennt keine Regel über Selektor-zu-Rumpf. `npm run lint` läuft grün, während der Satz falsch gesetzt ausgeliefert wird.

Das ist keine hypothetische Mutation. **Der Zustand am HEAD war die Regression.**

> **Behoben am 2026-08-30, noch in diesem Lauf.** Die Zeile trägt jetzt `class="hinweis"` — die geteilte Nebentext-Rolle, byte-gleich mit dem Regelkörper, den `.einmal__stand` verloren hat, und dieselbe, auf die `.einmal__warnung` im selben Commit umgestellt wurde. Die Waise ist aus dem `.aufnahme`-Kopf entfernt. `role="status"` bleibt.
>
> Und die Wache, die gefehlt hat, steht: `smoke-zugang.ts` behauptet jetzt, dass die Rückmeldung die geteilte Rolle trägt, keine eigene Klasse daneben führt und **kein Selektor ohne eigenen Rumpf** in der Datei steht. Der exakte Regressionszustand wurde als Mutation wiederhergestellt und ist rot geworden — alle drei Teile schlagen an. `smoke` 598 → **599**, volle Kette Exit 0.
>
> **Nicht** gebaut wurde die schärfere Fassung: „die meta-Rampe steht nur im geteilten Blatt" deckte diese ganze Klasse, ist heute aber zu Recht rot — `+page.svelte`, `NavBar.svelte` und `dienstplan/+page.svelte` tragen zusammen noch vier lokale Regeln mit `--meta-font`. Das gehört zu Posten R4.

**R2 · Zwei weitere Kommentare dieses Durchgangs behaupten mehr, als ihr Code tut.** *(fix now)*

- **Die Meldungsregion-Wache sagt „über alle Seitenkomponenten" und liest drei fest verdrahtete Dateien.** `smoke-zugang.ts:3409-3411` schreibt: „eine vierte Seite, die sich die Region ohne `tabindex` danebenschreibt, wäre nirgends aufgefallen. Gesucht wird jetzt über alle Seitenkomponenten." Darunter steht `['+page.svelte', 'dienstplan', 'verwaltung'].map(…)` (`:3413`). Die Liste aller sechs Seitenkomponenten existiert im selben Skript (`:5881`) und wird nicht benutzt. Die Lücke, die der Kommentar zu schliessen erklärt, ist offen: eine Region auf `/monatsplan` ohne `tabindex` liesse `meldungsTags.length === 3` unberührt.
- **Der alte Begründungsblock steht unverändert über dem neuen.** `smoke-zugang.ts:3390-3396` gegen `:3397-3411` — zwei aufeinanderfolgende Begründungen für dieselbe Zeile, die zweite widerspricht der ersten in der Reichweite.

**R3 · Die geteilte Rolle `.fliesstext` ist auf `/` wirkungslos — dieselbe Zeile steht auf zwei Seiten verschieden.** *(fix now)*

`+page.svelte:582` trägt `class="fliesstext zeile__text"`. Die lokale Regel `.zeile__text` (`:1069-1075`, `--task-*`-Rampe) schlägt nach Svelte-Bereichshash das globale `.fliesstext` (`bedienelemente.css:396-407`, `--body-*`-Rampe). Auf `/einzelaufgaben` hat dieselbe Zeile (`:57`) nur `.fliesstext`, und der `<style>`-Block dort ist leer. Ergebnis heute: **derselbe Titel steht auf `/` mit `line-height: 1.45` und auf `/einzelaufgaben` mit `1.55`** — genau die Divergenz, gegen die der Commit angetreten ist. Vor dem Commit gewann `.einzel__titel` (body-Rampe) per Quelltextreihenfolge; die Darstellung auf `/` hat sich also geändert.

**R4 · Von zehn neuen geteilten Rollen ist die Benutzung nur bei zweien behauptet.** *(**behoben am 2026-08-30** durch Gate-Regel 14 — siehe A3)*

Die `SEITENFORM`-Wache (`smoke-zugang.ts:6048-6053`) prüft `treffer.length === 1 && treffer[0].endsWith(STILBLATT)` — dass der Regel**kopf** in genau einer Datei steht. Nicht, dass irgendeine Seite die Klasse benutzt. Der Diff schliesst diese Lücke **nur** für die Zeilenformulare (`:6100-6107`), und der Kommentar dort sagt warum: „eine zurückgedrehte Klasse in `/dienstplan` kam grün durch." Für `.liste`, `.liste--getrennt`, `.fliesstext`, `.fliesstext--gedaempft`, `.knoepfe`, `.meldung`, `.nur-vorgelesen`, `.hinweis--ziffern` gibt es keine Benutzungsbehauptung. Der Zustand, den die erste Retrospektive gemessen hat — vierzehn byte-gleiche Regelkörper über sieben Komponenten —, kann in einem Commit zurückkehren, ohne dass etwas rot wird.

**R5 · Die Extraktion hat eine Wache mitgenommen.** *(defer)*

`font-variant-numeric: tabular-nums` war in `a25bda5` behauptet: `/\.woche__datum \{[^}]*font-variant-numeric: tabular-nums/`. Heute prüft dieselbe Zeile (`smoke-zugang.ts:6689`) nur noch das Markup. Die `SEITENFORM`-Wache trägt `.hinweis--ziffern`, prüft aber Kopf und Einmaligkeit, nicht den Rumpf. Die Deklaration aus `bedienelemente.css:459` löschen und `.hinweis--ziffern { }` stehen lassen bleibt grün — in vier Datumsanzeigen fällt die Tabellenstellung aus. Vor dem Commit wäre dieselbe Mutation rot geworden.

**R6 · `ERWARTETE_BEHAUPTUNGEN` zählt `pruefen`-Aufrufe und sieht Verluste *innerhalb* einer Teileliste nicht.** *(defer)*

`pruefhelfer.ts:55-63` zählt je Aufruf einmal. Der grösste Teil der Deckung liegt aber in Sammelbehauptungen (`formTeile` mit zwanzig Einträgen, `planTeile`, `zusageTeile`, …). Nachgezählt: 246 Aufrufe am HEAD gegen 240 in `a25bda5` — auf Aufrufebene kein versteckter Verlust. Der Verlust in R5 liegt eine Ebene tiefer und hat die Konstante nicht bewegt. Der Docblock (`:193-201`) beschreibt die Zahl als Wache gegen „stillschweigend weniger Deckung"; sie deckt die grobkörnigste Ebene.

**R7 · Beim Zusammenlegen ist die tragende Warnung aus dem Kommentar gefallen.** *(fix now — billig)*

Beide lokalen Fassungen des Griffs trugen sie. `git show a25bda5:verwaltung/+page.svelte:814-817`:

> „Die Voreinstellung `display: list-item` bleibt stehen: sie ist es, die das Dreieck malt, und das Dreieck ist die einzige Anzeige, dass hier etwas aufgeht. `display: flex` nähme es weg — dann sähe der Griff aus wie ein Satz Text."

Der geteilte `.zeilenform__griff` (`bedienelemente.css:608-621`) sagt nur noch: „Trefferfeld über die volle Breite und 44px hoch, Text in der Akzentfarbe." `min-height: var(--touch)` an einem `list-item` zentriert nicht vertikal — der naheliegende nächste Griff ist `display: flex; align-items: center`, und der entfernt das Dreieck jetzt auf **beiden** Seiten zugleich. Die Warnung, die genau das verhinderte, ist fort, und keine Behauptung deckt sie.

Das ist der eigentliche Preis einer Zusammenlegung: nicht die Zeilen, die verschwinden, sondern das Wissen, das mit der zweiten Kopie verschwindet.

**R8 · Kleinkram aus derselben Fläche.** *(defer)*

- `einzelaufgaben/+page.svelte:78-79` — ein leerer `<style></style>`-Block; alle vier lokalen Regeln sind gezogen, der Block gehört mit.
- `dienstplan/+page.svelte:191` — `class="karte woche"` trägt einen toten Token; `.woche` ist nirgends definiert. **Vorbestehend**, aber `.woche` wurde in genau diesem Durchgang zu `.karte`, und der Token blieb liegen. Die adversariale Schicht hat alle sieben Komponenten gegen lokale und globale Selektoren abgeglichen: es ist der **einzige** undefinierte Token im Baum.
- `bedienelemente.css:491-493` zählt „siebenmal im Baum, unter **sieben** Namen" und listet dann fünf (`.liste` dreimal). Sieben Vorkommen, fünf Namen. In einer Datei, deren ganze Beweisführung auf dieser Unterscheidung ruht, lohnt die Korrektur.
- `dienstplan/+page.svelte:368-387` — zwei aufeinanderfolgende, unverschmolzene Kommentarblöcke vor `.woche__jahr`; der zweite stammt aus Durchgang 2 heute. Streng genommen fehlt gegenüber `.hinweis hinweis--ziffern` neben `line-height` auch `margin: 0` — an einem `<span>` folgenlos, aber „**eine** Eigenschaft Unterschied" stimmt damit nicht.

**R9 · Barrierefreiheit: geprüft und sauber.** *(accept)*

Alle sieben Komponenten am HEAD durchgesehen: `aria-labelledby` an `<summary>` und `<ul>`, `role` / `aria-live` / `tabindex="-1"` an allen dreizehn Live-Regionen, `open={fehlerHier}`, `bind:this`, `fokusNach` unverändert. Die drei Meldungsregionen tragen weiterhin dieselben fünf Attribute. Die Fokusreihenfolge in den Dialogen bleibt: `.knoepfe` ist byte-gleich mit den drei ersetzten Regeln, `Abbrechen` steht weiterhin zuerst im DOM. Jede einzelne Ersetzung wurde gegen `a25bda5` verglichen; ausser R1 und R3 ist keine geteilte Regel nicht byte-gleich, und der Spezifitätswechsel global↔gescopt kollidiert nur bei `.zeile__text`.

Das ist eine **geprüfte** Aussage, keine Vermutung — und der Gegenbeleg dazu, dass dieser Durchgang nur Schaden angerichtet hätte.

### S — Der Review zu Story 3.2 (heute)

**S1 · Der abgesicherte Bereich der Story war der Ausfallweg.** *(behoben)*

Zwei Mutationen gingen vor dem Review grün durch und sind jetzt rot: `cancel()` in `versandFragen` streichen (die Person sähe Dialog **und** Frageblock nebeneinander) und den `{#if}`-Zweig auf `/einzelaufgaben` kollabieren (die einzige Auskunft der Seite fällt aus). Zehn Patches gebaut, vier Mutationen einzeln rot gesehen. Vollständig in der Spec, Abschnitt `Review Findings`, Durchgang 2.

**S2 · Eine Behauptung aus diesem Review ist über einer leeren Liste vakuant wahr.** *(fix now — eigener Fund)*

`smoke-zugang.ts:7593-7596`: `unterSrc.filter(d => d.text.includes('steht danach für alle daneben')).every(…)`. Setzt jemand `UEBERNAHME_FOLGE` auf `''`, bleibt kein Treffer übrig, `.every()` über `[]` ist `true`, und die zwei Geschwisterteile lesen `{UEBERNAHME_FOLGE}` im Markup, nicht den Wert. Die Zusage „die zwei Bestätigungswege sagen dasselbe" hielte dann, indem beide **nichts** sagen. `smoke-http.ts` sucht den Satz nie. Die Entscheidung von heute — die Folge ist Substanz — ist von keiner ausgeführten Behauptung gedeckt.

### A — Architektur-Delta

**A1 · Der Quellbaum ist nachgezogen — und die Namensregel darüber widerspricht ihm jetzt.** *(fix now — Spec-Abgleich; **kein** Epic-4-Blocker, siehe Nachtrag)*

Aktionspunkt 37 ist gehalten: alle siebzehn zuvor fehlenden Module stehen im Baum (geprüft, jedes einzeln), `mehr/` ist da, CAP-6 nennt beide Routen, und ein Abweichungsblock (`ARCHITECTURE-SPINE.md:335-343`) hält fest, dass `queries/dutyWeeks.ts` in Wahrheit `duty-weeks.ts` heisst, „kebab-case wie alle Nachbarn".

Die **Regel**, die den falschen Namen erzeugt hat, ist 190 Zeilen darüber unverändert geblieben:

> `:148` | Repository-Dateien | camelCase, nach Domäne benannt: `tasks.ts`, `dutyWeeks.ts`, `signupTasks.ts`, `sheets.ts`, `members.ts` |

Gebaut sind `duty-weeks.ts`, `signup-tasks.ts`, `tasks.ts`, `members.ts` — **kebab-case, alle vier**. Das Dokument notiert die Abweichung an einer Stelle und schreibt sie an einer anderen weiter vor.

> **Nachtrag vom 2026-08-30, und eine Korrektur an mir selbst.** Dieser Befund stand hier zuerst als Epic-4-Blocker, weil die Regel `sheets.ts` namentlich nennt — die Datei, die Story 4.1 anlegt. Das ist beim Nachsehen nicht haltbar: `sheets` ist **ein Wort** und schreibt sich unter kebab-case wie unter camelCase gleich. Story 4.1 stolpert nicht darüber. Die falsche Regel trifft erst die nächste **mehrteilige** Repository-Datei.
>
> Der Befund bleibt — eine Regel, die dem Baum und einem Abweichungsblock im selben Dokument widerspricht, gehört korrigiert, bevor jemand sie befolgt. Er ist nur kein Blocker. **Behoben am 2026-08-30:** die Zeile steht auf kebab-case, mit dem Grund daneben.

**A2 · Keine Schichtverletzung — geprüft und sauber.** *(accept)*

`npm run gate` meldet 0 Hinweise über 44 Dateien und 62 Tokens, dreizehn Regeln erfüllt; `gate --selftest` findet alle 29 Fehlerproben, jede Regel beisst nachweislich. Unverändert gegenüber dem ersten Lauf.

**A3 · Der Entscheid über das Prüfwerkzeug ist jetzt drei Retrospektiven alt.** *(**entschieden am 2026-08-30** — siehe Nachtrag)*

| | Epic-1-Retro | Epic-2-Retro | Epic-3-Retro | heute |
| --- | ---: | ---: | ---: | ---: |
| `scripts/` | — | 6 512 | 12 140 | **12 523** |
| `src/` | — | — | 9 738 | 10 008 |
| Verhältnis | — | — | 1,25 : 1 | **1,25 : 1** |

Der Punkt steht seit `epic-1-retro-item-7` offen, wurde in Epic 2 (`item-23`) und Epic 3 (`item-36`) wiederholt. Das Verhältnis ist stabil — das Werkzeug wächst mit dem Produkt, es läuft ihm nicht davon. Aber `ARCHITECTURE-SPINE.md` sagt unter *Deferred* weiterhin „**Testframework.** Die Referenz hat keins", über einem Projekt, das sich in drei Epics eines gebaut hat und dessen ganze Abnahme darauf ruht. Dieser Lauf liefert dazu ein neues Argument in **beide** Richtungen: das Werkzeug hat heute R1 **nicht** gefunden, und es hat in Durchgang 2 vier Mutationen rot gemacht.

> **Entschieden am 2026-08-30.** Nicht aufnehmen und nicht zurückbauen, sondern **trennen** — die zwei vorgelegten Antworten waren beide falsch, und darum blieb der Punkt dreimal liegen. Vollständig in `ARCHITECTURE-SPINE.md` unter *Prüfwerkzeug*; die drei Aktionspunkte aus Epic 1, 2 und 3 sind geschlossen.
>
> **Und eine Zahl, die den Entscheid dreimal zu freundlich gerahmt hat.** Die 1,25 : 1 sind Gesamtzeilen. Dieses Projekt kommentiert sehr ungleich: `scripts/` ist zu 63 % Code, `src/` nur zu 43 % — der Produktcode ist zur Hälfte Prosa. **Auf Code allein steht das Verhältnis bei 1,85 : 1.** Das Werkzeug ist fast doppelt so gross wie die Anwendung, nicht ein Viertel grösser.
>
> Der Entscheid: Verhalten (`smoke-http.ts`, der aufrufende Teil von `smoke-zugang.ts`, der Prüfkern) wird Architekturbestandteil. **Regeln über den Quelltext sind Lint und ziehen nach `gate.mjs`**, wo das Modell schon steht: nummerierte Regeln, jede durch eine Fehlerprobe belegt bissig.
>
> **Erster Umzug, gebaut:** die Wache über die geteilten Gestaltungsrollen ist **Gate-Regel 14**, mit vier Fehlerproben — und hat dabei die Form gewechselt. Vorher eine von Hand geführte Liste von zwanzig Namen, die kuratiert werden musste und `.hinweis--ziffern` bis zu seinem Review übersah; jetzt aus dem Stilblatt **abgeleitet**, über Regel**körper** statt Namen. Das schliesst **R4** mit: eine Seite, die auf ihre eigene Klasse zurückdreht, muss die alte Regel wieder hinschreiben, und der gleiche Regelkörper fällt auf — an einer ausgeführten Mutation belegt. Zusätzlich fängt sie den Zwilling **innerhalb** des Blatts, den eine Wache, die Dateien zählt, prinzipiell nicht sehen kann (**B1**).
>
> **NFR13 ist präzisiert:** „Es gibt kein Testframework" wurde über drei Retrospektiven als Beleg gelesen, dass dieses Projekt nicht prüfe. Gemeint war immer die **Abhängigkeit**. Der Satz sagt das jetzt.
>
> **Was offen bleibt:** Stufe C. Nichts im Werkzeug liest Regelkörper gegen ihre Absicht oder gerendertes Ergebnis — R1 bleibt ausserhalb aller drei Schichten. Die Auslösebedingung ist eingetreten, der Preis ist eine fremde Abhängigkeit, und die Entscheidung ist als eigener Punkt im Register.

### B — Duplikate

**B1 · Die Duplikate zwischen den Komponenten sind wirklich fort — und der ausdrücklich benannte `.fehler`-Zwilling ist es nicht.** *(zur Hälfte accept, zur Hälfte fix now)*

Der Commit `4af054f` behauptet: „die Duplikatsmessung fällt von vierzehn auf zwei". **Über die Komponenten stimmt das.** Eigene Messung über alle `<style>`-Blöcke unter `src/`, byte-gleiche Regelkörper mit mindestens zwei Deklarationen: **einer** — und genau der, den der Commit als absichtliche Zufallsgleichheit benennt (`.erfassen` / `.schreiben` / `.pruefen` / `.aufnahme` mit `display:flex; flex-direction:column; gap:var(--space-3)`).

**Meine Messung hatte einen blinden Fleck, und die adversariale Schicht hat ihn gefunden.** Sie las `<style>`-Blöcke in Komponenten und konnte darum ein Duplikat **innerhalb** des geteilten Stilblatts strukturell nicht sehen. Dort steht eines:

```css
.fehler {                        .fliesstext, .leer {
  margin: 0;                       margin: 0;
  color: var(--ink-primary);       font-family: var(--body-font);
  font-family: var(--body-font);   font-size: var(--body-size);
  font-size: var(--body-size);     font-weight: var(--body-weight);
  font-weight: var(--body-weight); line-height: var(--body-line);
  line-height: var(--body-line); }
}                                .fliesstext { color: var(--ink-primary); }
```

`bedienelemente.css:233-240` gegen `:396-407`. Derselbe Regelkörper, zweimal, in **derselben Datei**, 163 Zeilen auseinander. Aktionspunkt 38 nannte den Fall wörtlich — „zuerst die Zwillinge von `.fehler`, `.hinweis` und `.leer`" —, `.hinweis` und `.leer` sind gezogen, `.fehler` nicht, und der Punkt steht auf `done`. Der Kommentar bei `:382` schreibt dazu: „Die Retrospektive hat das gemessen; **hier steht es einmal**."

Die `SEITENFORM`-Wache kann das prinzipiell nicht fangen: sie zählt **Dateien** (`unterSrc.filter(datei => muster.test(datei.text))`). Ein Duplikat innerhalb des Stilblatts ist für sie unsichtbar — dieselbe Klasse blinder Fleck wie in meiner eigenen Messung, und derselbe wie R1 und R5: niemand liest Regel**rümpfe**.

Was trotzdem steht: über sieben Komponenten hinweg ist das Problem gelöst, unabhängig nachgemessen. Der Rest ist ein Zwilling im Stilblatt selbst.

**B2 · Der Produktcode ist geschrumpft.** *(accept)*

| Datei | `a25bda5` | heute |
| --- | ---: | ---: |
| `routes/+page.svelte` | 1 207 | **1 173** |
| `routes/verwaltung/+page.svelte` | 836 | **751** |
| `routes/dienstplan/+page.svelte` | 464 | **415** |
| `lib/styles/bedienelemente.css` | 465 | 632 |

`src/` netto **−52** Zeilen über den ganzen Bereich seit dem ersten Lauf. Befund C1 des ersten Laufs (`+page.svelte` bei 1 207) hat sich ohne eigenen Aktionspunkt entspannt.

### D — Buchführung

**D1 · Die Sprache der Domänenspalten ist weiter ungeklärt, und Epic 4 legt als nächstes Tabellen an.** *(fix now — Entscheid durch Manuel, blockiert Epic 4)*

| Tabelle | Spalten | Epoche |
| --- | --- | --- |
| `members` | `id, name, invite_token_hash, is_admin, is_active, created_at` | englisch |
| `tasks` | `id, text, completed_by, completed_at, due_at, created_at` | englisch |
| `duty_weeks` | `id, art, **iso_jahr**, **iso_woche**, member_id, created_at` | gemischt |
| `signup_tasks` | `id, **titel**, **termin_at**, member_id, created_at` | gemischt |

Epic 4 Story 4.1 heisst „Referenz-Sheets lesen und schreiben" und legt die nächste Tabelle an, mit einem Titel und einem Freitext — sie muss also genau diese Spalten benennen.

> **Entschieden am 2026-08-30: Domänenspalten deutsch, Infrastrukturspalten englisch.** `sheets(id, titel, text, created_at)`. Festgehalten in `ARCHITECTURE-SPINE.md` unter *Consistency Conventions* und an Story 4.1 in `epics.md`.
>
> Beim Vorbereiten des Entscheids fiel auf, dass die Diagnose „nach Epoche gespalten" — aus der Epic-2- wie der Epic-3-Retrospektive — so nicht stimmt. **Epic 1 hat nie gewählt.** Seine einzigen zwei Domänenspalten heissen `name` und `text`, und beide Wörter sind im Deutschen wie im Englischen identisch geschrieben. Alles übrige aus Epic 1 und 2 ist Infrastruktur und war nie strittig. Es gibt damit genau **einen** wirklich getroffenen Präzedenzfall, und der ist deutsch. Der Entscheid folgt ihm, statt eine Spaltung aufzulösen, die es nie gab.
>
> **Eine Warze wird benannt und nicht geheilt:** `duty_weeks.art` ist die schwächste Spalte im Baum — ein englischsprachiger Leser liest „Kunst". Der ehrlichere Name wäre `dienstart`. Sie bleibt; eine Migration allein für einen Spaltennamen ist den Preis nicht wert.

**D2 · Die von Hand gepflegten Zahlen sind von fünf auf sechs gewachsen.** *(defer)*

Neu seit heute: `rueckrufe.length >= 8` (`smoke-zugang.ts:6301`) mit der ausgeschriebenen Entsprechung „**Acht** Wurfstellen" in `texte.ts:109`. Dazu bewegt: `ERWARTETE_BEHAUPTUNGEN` 592 → 597 und 140 → 141. Die Reibung ist Absicht; sie skaliert mit dem Werkzeug, und das Werkzeug wächst.

**D3 · Die zwei Zähler aus Bucket D sind nicht nur veraltet, sie sind jetzt falsch.** *(fix now — reine Buchführung)*

`deferred-work.md:617-620` schreibt: `request.formData()` ohne try/catch „steht weiterhin an **sieben** Stellen", harte Redirect-Pfade „an **sechs**". Gemessen am HEAD: **11** und **7**. Der erste Lauf hat die Bewegung schon gemeldet (Befund D3, Aktionspunkt 45); seither hat niemand die Zahl angefasst. Ein Dokument, das eine gemessene Zahl um vier verfehlt, ist schlechter als eines, das keine nennt.

### E — Spec-Abgleich

**E1 · FR-Deckung vollständig, an ausgeführten Behauptungen.** *(accept)*

FR6–FR10 über die Stories 3.1 und 3.2, NFR13 über Story 3.0. `smoke:http` misst 142 Behauptungen am **gebauten** Server über echtes HTTP, darunter der zweischrittige POST ohne JavaScript, der Diensthinweis, die laufende Woche und — seit heute — `/einzelaufgaben` mit beiden Zuständen nebeneinander.

**E2 · Der `/mehr`-Eintrag ist heute umbenannt worden, und `epics.md` ist damit zwei Schritte hinten.** *(fix now — Spec-Abgleich)*

`epics.md:499` nennt **einen** Eintrag (`Einzelaufgabe ausschreiben`); gebaut sind **zwei**. Der zweite hiess bis heute `Übernommene Einzelaufgaben` und heisst jetzt `Alle Einzelaufgaben` (Entscheid im Review, `mehr/+page.svelte:50`). Aktionspunkt 41 stand schon vor dieser Umbenennung offen. Zusätzlich trägt die abgehakte Aufgabenzeile der Story-Spec (`:102`) weiter die alte Beschriftung, und für die Umbenennung selbst gibt es — anders als für die zwei anderen Abweichungen von heute — **keinen** Eintrag im Spec Change Log. Das ist ein Fehler in der Arbeit dieses Vormittags.

### F — Prozess

**F1 · Die Aktionspunkte 4 und 5 wurden gegen einen anderen Massstab geschlossen als ihren Wortlaut — und die tragende Begründung dafür hält nicht.** *(fix now)*

Aktionspunkt 39 verlangte „die Live-Region als geteilte **Komponente**", Aktionspunkt 40 „das Zeilenformular **zusammenlegen**". `src/lib/components/` enthält `NavBar.svelte` und `TitleBar.svelte` — keine der beiden. Geteilt wurde auf CSS-Ebene; das Markup steht weiter je dreimal (`+page.svelte:513,516`, `dienstplan:174,177`, `verwaltung:364,367`) bzw. je zweimal (`dienstplan:253-262`, `verwaltung:522-531`).

Für Aktionspunkt 40 ist die Begründung stichhaltig und nachgeprüft: das `<form>` muss in seiner Route bleiben, weil **Gate-Regel 11 die Route aus dem Verzeichnis der Datei ableitet** — eine Komponente unter `src/lib/components/` hat keins, die Regel würde blind. Das ist ein echter Grund, und er ist wichtig genug, dass ihn jemand kennen muss, bevor er die Komponente doch baut.

**Für Aktionspunkt 39 hält sie nicht.** `bedienelemente.css:564-566` begründet den Verzicht so:

> „Statt dessen prüft `smoke` **jede** Region des Baums gegen ihre Attribute. Das ist die schärfere Zusage: eine Komponente hindert niemanden daran, sich die Region von Hand danebenzuschreiben — die Behauptung schon."

Gemessen am HEAD trägt der Baum **dreizehn** Elemente mit `live`: neun `fehler live`, drei `meldung live`, eine `hinweis--am-feld live`. Geprüft werden die drei `meldung`-Regionen — und die auch nur über drei fest verdrahtete Pfade (R2). Einen baumweiten Durchlauf über `aria-live` gibt es nicht. Die Zusage, die als *schärfer als eine Komponente* verkauft wurde, deckt drei von dreizehn, und genau der Handschriften-Fall, den sie ausschliessen soll, kommt durch.

Dazu die Buchführung: ein Leser des Registers liest „Live-Region als geteilte Komponente — done" und schliesst vernünftigerweise auf eine Komponente. Die Begründung — die richtige für 40, die falsche für 39 — lebt allein in der Commit-Nachricht von `4af054f`.

**F2 · Die Aktionspunkte sind ohne Review-Durchgang gelandet.** *(fix now — Prozess; die Ursache von R1, R2, R3)*

Fünf Commits, 19 Dateien, sieben Komponenten gleichzeitig angefasst — und kein Review. Story 3.2 bekam drei Review-Schichten im Bau plus vier heute; ihr eigener Aktionspunkt-Nachlauf bekam keine. Die drei Befunde R1, R2 und R3 stammen alle aus diesen fünf Commits, und R1 ist eine ausgelieferte Regression. Ein Refactoring über sieben Komponenten ist genau die Änderungsform, bei der eine Regel für eine Seite still ihre Bedeutung wechselt, die niemand noch einmal gelesen hat.

**F3 · Der Spec Change Log ist einmal angewandt und nicht zur Regel erhoben.** *(fix now — Prozess)*

Aktionspunkt 43 verlangt, den Spec Change Log zur Regel zu erheben, statt auf Sitzungsprotokolle zu warten. Durchgang 2 hat zwei Abweichungen nachgetragen und den Posten dabei ausdrücklich zitiert — und die dritte Abweichung desselben Vormittags (die `/mehr`-Umbenennung, E2) **nicht** eingetragen. Eine Praxis, die man beim dritten Fall am selben Tag vergisst, ist keine Regel.

**F4 · Sitzungsprotokolle: unverändert keine.** *(defer — siehe F3)*

---

## Verhaltensprüfung

**Ausgeführt**, nicht behauptet:

| Lauf | Ergebnis |
| --- | --- |
| `npm run check` | Exit 0, 649 Dateien, 0 Fehler, 0 Warnungen |
| `npm run lint` (volle Kette) | **Exit 0** |
| `gate` / `gate --selftest` | 0 Hinweise über 44 Dateien; 29 Fehlerproben, jede der 13 Regeln beisst |
| `db:check` / `--selftest` | grün |
| `smoke` | **598** Behauptungen |
| `smoke:selftest` | 15 Behauptungen |
| `smoke:http` | **142** Behauptungen am **gebauten** Server über echtes HTTP |

Dazu **vier einzeln ausgeführte Mutationen** in Durchgang 2, jede vorher grün und nachher rot (`cancel()`, der `{#if}`-Zweig auf `/einzelaufgaben`, `{UEBERNAHME_FOLGE}` im No-JS-Weg, `.hinweis--ziffern` zurück in eine Komponente).

**Nicht ausgeführt:** kein kopfloser Browser. Und die Ehrlichkeit dieses Abschnitts ist begrenzt durch R1 — die ganze grüne Kette oben lief über eine ausgelieferte Regression hinweg.

---

## Abgleich mit dem ersten Lauf

| Befund vom 2026-08-29 | Aktionspunkt | Stand | Beleg |
| --- | --- | --- | --- |
| A1 Quellbaum kennt 17 Module nicht | 37 | **gehalten** | alle 17 im Baum, einzeln geprüft |
| A2 Drei Namen treffen die Wirklichkeit nicht | 37 | **gehalten**, mit Rest | Abweichungsblock `:335-343`; die Regel `:148` blieb — siehe A1 dieses Laufs |
| A3 Prüfwerkzeug-Entscheid | 36 | **offen**, 3. Retro | 12 523 : 10 008 |
| B1 14 byte-gleiche Regelkörper | 38 | **gehalten**, nachgemessen | eigene Messung: 1 übrig, absichtlich |
| B2 Live-Regionen verhaltensgleich | 39 | **anders geschlossen** | CSS statt Komponente — F1 |
| B3 Zeilenformular kopiert | 40 | **anders geschlossen** | CSS statt Komponente — F1 |
| C1 `+page.svelte` bei 1 207 | — (defer) | **entspannt** | 1 173 |
| C2 `smoke-zugang.ts` bei 7 386 | siehe A3 | **gewachsen** | 7 699 |
| D1 Spaltennamen nach Epoche gespalten | 42 | **offen**, blockiert Epic 4 | vier Tabellen gemessen |
| D2 Handgepflegte Zahlen drei → fünf | — (defer) | **fünf → sechs** | `rueckrufe.length >= 8` |
| D3 Zwei Zähler bewegt | 45 | **offen und jetzt falsch** | 7/6 im Dokument, 11/7 gemessen |
| E2 `/mehr` zwei Einträge statt einem | 41 | **offen, gewachsen** | dazu heute umbenannt |
| E3 Zwei AC in Spannung | 41 | **offen** | `epics.md` unverändert |
| F1 Keine Sitzungsprotokolle | 43 | **einmal angewandt** | zwei Einträge, der dritte Fall vergessen |
| F2 Handprüfungen zweierlei abgenommen | 44 | **offen** | keine Regel geschrieben |

**Vier von zehn Aktionspunkten sind erledigt** (37, 38, 39, 40), und drei davon haben wirklich getragen. Sechs stehen offen; zwei davon (36, 42) blockieren Epic 4.

### Vorschläge für `--set-action-status`

Keine. Die vier erledigten stehen bereits auf `done`; die sechs offenen sind belegt offen. Nichts vorzuschlagen.

---

## Aktionspunkte

1. ~~**Den Regelkörper von `.einmal__stand` wiederherstellen**~~ — **erledigt am 2026-08-30**, mit Wache und roter Mutation. — `verwaltung/+page.svelte:718`. Am ehrlichsten im Sinn des Commits: die Zeile auf `class="hinweis"` umstellen, dann trägt die bestehende `SEITENFORM`-Wache sie. *Owner: Manuel.* **(R1 — blockierend)**
2. **Die Meldungsregion-Wache über `seitenKomponenten` führen** statt über drei fest verdrahtete Pfade, und den doppelten Kommentarblock auflösen — `smoke-zugang.ts:3390-3437`. *Owner: Manuel.* **(R2)**
3. **`.zeile__text` gegen `.fliesstext` auflösen** — dieselbe Zeile darf auf `/` und `/einzelaufgaben` nicht zwei Zeilenhöhen haben. *Owner: Manuel.* **(R3)**
4. **Die Literal-Behauptung zu `UEBERNAHME_FOLGE` auf eine nichtleere Trefferliste bestehen lassen**, und den Satz in `smoke-http.ts` am ausgelieferten Dokument suchen. *Owner: Manuel.* **(S2)**
5. ~~**Entscheid: das Prüfwerkzeug**~~ — **getroffen am 2026-08-30**: trennen statt aufnehmen/zurückbauen. Regel 14 als erster Umzug gebaut. Schliesst die Punkte aus Epic 1, 2 und 3. **(A3)**
6. ~~**Entscheid: die Sprache der Domänenspalten**~~ — **getroffen am 2026-08-30**: Domäne deutsch, Infrastruktur englisch. In Spine und `epics.md` festgehalten. **(D1)**
7. ~~**Die Namensregel `ARCHITECTURE-SPINE.md:148` auf kebab-case ziehen**~~ — **erledigt am 2026-08-30**. Kein Epic-4-Blocker gewesen: `sheets.ts` ist ein Wort. **(A1)**
8. **Die zwei Zähler in `deferred-work.md:617-620` auf 11 und 7 setzen.** Reine Buchführung; die Zahlen sind heute falsch, nicht nur alt. *Owner: Manuel.* **(D3)**
9. **`epics.md` für Story 3.2 abgleichen**: AC 3 auf `/einzelaufgaben` beziehen, den zweiten `/mehr`-Eintrag nachtragen — jetzt unter dem Namen `Alle Einzelaufgaben` —, und die Umbenennung im Spec Change Log nachtragen. *Owner: Manuel.* **(E2, E3)**
10. **Aktionspunkt-Nachläufe bekommen einen Review**, wie Stories einen bekommen. Fünf Commits über sieben Komponenten sind ohne Durchgang gelandet, und drei Befunde dieses Laufs stammen daraus. *Owner: Manuel.* **(F2 — die Prozesslehre dieses Laufs)**
11. **Die Begründung einer abweichend geschlossenen Aktion gehört ins Register, nicht nur in die Commit-Nachricht** — mindestens ein Verweis auf den Commit im `action_items`-Eintrag. *Owner: Manuel.* **(F1)**
12. **Den `.fehler`-Zwilling ins geteilte `.fliesstext` ziehen** — `bedienelemente.css:233-240` gegen `:396-407`, byte-gleich, in derselben Datei. Aktionspunkt 38 nannte ihn wörtlich und steht auf `done`; der Kommentar bei `:382` behauptet, es stehe einmal. *Owner: Manuel.* **(B1)**
13. **Die `list-item`-Warnung in den geteilten `.zeilenform__griff` zurückholen** — sie stand in beiden lokalen Fassungen und ist beim Zusammenlegen verschwunden. *Owner: Manuel.* **(R7)**
14. **Den leeren `<style>`-Block und den toten `.woche`-Token entfernen**, dazu die zwei Kommentar-Ungenauigkeiten. *Owner: Manuel.* **(R8)**
15. **Eine Regel für die Abnahme der Handprüfungen**: durchgeführt und datiert, oder ausdrücklich als ungeprüft abgenommen. Unverändert offen aus dem ersten Lauf. *Owner: Manuel.* **(F2 des ersten Laufs)**

**In `sprint-status.yaml` geschrieben: zehn.** Die fünf, die schon aus dem ersten Lauf offenstehen, sind **nicht** dupliziert worden — sie stehen dort unter ihren alten Ids und gelten weiter:

| Hier | Bereits im Register |
| --- | --- |
| 5 · Entscheid Prüfwerkzeug | `epic-3-retro-item-36` |
| 6 · Entscheid Spaltensprache | `epic-3-retro-item-42` |
| 8 · Die zwei Zähler in Bucket D | `epic-3-retro-item-45` |
| 9 · `epics.md` für Story 3.2 | `epic-3-retro-item-41` |
| 15 · Regel für die Handprüfungen | `epic-3-retro-item-44` |

Dazu unverändert offen: `epic-3-retro-item-43` (Spec Change Log zur Regel erheben) — siehe F3.

---

## Akzeptanz-Urteil

**Maschineller Spruch: `accepted-with-open-items`.** Kriterien: **deklariert** (`epics.md:434-546`).

`pending_stories` ist leer — der Grund des ersten Spruchs ist fort. Damit greift die Regel, die ihn erzwungen hat, nicht mehr, und es bleibt die Frage nach den Kriterien selbst.

**Dafür, belegt:**

- **Alle fünf funktionalen Anforderungen sind gebaut und gemessen** (FR6–FR10), dazu NFR13 über Story 3.0.
- **Kein Akzeptanzkriterium von Epic 3 ist verletzt.** Die zwei Abweichungen sind eine Erweiterung (E2) und eine Auflösung eines Widerspruchs in den Kriterien selbst (E3).
- **755 ausgeführte Behauptungen** über drei Skripte, davon 142 am gebauten Server über echtes HTTP; `gate` ohne Hinweis; `check` und `lint` Exit 0.
- **Story 3.2 hat ihren separaten Review bekommen** — die Bedingung, die der erste Lauf gestellt hat, ist erfüllt: vier Schichten, zehn Patches, vier Mutationen rot.
- **Drei Aktionspunkte haben wirklich getragen**, unabhängig nachgemessen: die Duplikatszahl fällt von vierzehn auf einen, und der Produktcode ist um 52 Zeilen geschrumpft.

**Dagegen, und darum nicht `accepted`:**

- **R1 war eine ausgelieferte Regression** — behoben, noch in diesem Lauf, mit einer Wache und einer roten Mutation. Sie lag ausserhalb der Kriterien von Epic 3 (`/verwaltung` ist Story 1.3), wurde aber vom Nachlauf **dieses** Epics verursacht. Dass sie behoben ist, ändert den Spruch nicht: das Epic hat sie ausgeliefert, und 755 Behauptungen sind darüber hinweggelaufen.
- **Ein Entscheid steht weiter offen und ist drei Retrospektiven alt** (A3, das Prüfwerkzeug). Die zwei Namensentscheide (D1, A1) sind am 2026-08-30 getroffen und in den Plandokumenten festgehalten; A1 war entgegen der ersten Fassung dieses Dokuments **nie** ein Epic-4-Blocker.
- **Zwei Aktionspunkte stehen auf `done` und sind es nicht ganz.** Punkt 38 liess den ausdrücklich benannten `.fehler`-Zwilling stehen (B1); Punkt 39 wurde mit einer Begründung geschlossen, die nachweislich nicht hält (F1). Beide sind keine Kriterienverletzung — sie sind ein Loch in der Buchführung, an dem eine spätere Retrospektive vorbeiliest.

**`rejected` wäre falsch:** die Kriterien sind erfüllt und gemessen, alle Stories sind fertig, und kein Befund verletzt eine Zusage dieses Epics. **`accepted` wäre unehrlich:** dieses Epic hat über seinen eigenen Nachlauf eine Regression ausgeliefert, die durch 755 Behauptungen gelaufen ist, ohne rot zu werden.

**Menschliche Entscheidung: ausstehend.** Der Spruch steht bis dahin als `accepted-with-open-items`.

---

## Offene Fragen

- **Was hat R1 wirklich verursacht — die Änderung oder das Fehlen des Reviews?** Der Fehler selbst ist trivial: ein Selektor, dem beim Löschen seines Partners der Rumpf abhandenkam. Ihn hätte jedes Augenpaar gesehen. Die Frage ist nicht, wie er entstand, sondern warum fünf Commits über sieben Komponenten ohne Durchgang landen durften, während eine einzelne Story sieben bekommt.
- **Ist eine Wache über Selektor-zu-Regelkörper zu bauen?** R1, R4, R5 und B1 sind derselbe blinde Fleck: die Prüfskripte lesen Regel**köpfe** und Markup, nie Regel**rümpfe**. Das ist die Klasse, für die es heute gar kein Werkzeug gibt — und sie hängt am Entscheid A3. **Auch meine eigene Duplikatsmessung in diesem Lauf hatte ihn**: sie las `<style>`-Blöcke in Komponenten und konnte den Zwilling im Stilblatt strukturell nicht sehen. Vier unabhängige Messungen mit demselben Rand ist kein Zufall, sondern eine fehlende Werkzeugklasse.
- **Trägt der Spec Change Log ohne eine Regel, die ihn erzwingt?** F3 zeigt: zweimal angewandt, beim dritten Fall desselben Vormittags vergessen.
